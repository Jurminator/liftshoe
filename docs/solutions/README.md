# Solutions

Non-obvious learnings from work in this repo: root causes, platform gotchas, decisions and their reasons.
One file per topic, YAML frontmatter with `title`, `tags`, `date`. Written by `/compound`, read by agents before planning.
